Perfect — both VC++ redistributables are installed correctly now. The versions look good too.

Next steps:

1. Restart your computer completely.
2. Open PowerShell again.
3. Activate your venv:

```powershell
venv\Scripts\activate
```

4. Test Torch directly:

```powershell
python
```

Then inside Python:

```python
import torch
print(torch.__version__)
```

If it prints something like:

```python
2.4.0+cpu
```

then the DLL issue is fixed.

After that:

```powershell
uvicorn app.main:app --reload
```

---

If `import torch` still throws the same `fbgemm.dll` error after reboot, then switch to the more compatible Torch build:

```powershell
pip uninstall torch -y
pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
```

That version is often more stable on some Windows CPUs.

You’re down to the final environment compatibility issue now.
